{% extends "layout.njk" %}

{% block content %}
<div class="card table-container">
    <a href="/pacientes/create">Novo</a>
  <table width="100%">
    <thead>
      <tr>
        <th>Paciente</th>
        <th>Terapias </th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      {% for paciente in pacientes %}
      <tr>
        <td>
          <span
            style="background-image: url({{ paciente.avatar_url }})">
          </span>
          {{ paciente.name }}</td>
        <td>{{ paciente.services }}</td>
        <td><a href="/pacientes/{{ paciente.id }}">Ver</a></td>
      </tr>
      {% endfor %}
    </tbody>
  </table>
</div>
{% endblock content %}